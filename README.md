# tapiz-connector
tapiz-connector/ │ ├── signals.json          # entrada de señales ├── state.json            # estado interno de la neurona ├── neuron.py             # motor de la neurona ├── config.json           # configuración (wallet, repo destino, etc) │ └── .github/     └── workflows/         └── neuron.yml    # ejecutor automático
